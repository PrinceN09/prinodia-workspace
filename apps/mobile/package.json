{
  "name": "@prinodia/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Prinodia Workspace Mobile Application — React Native (planned)",
  "scripts": {
    "dev": "echo 'Mobile app (React Native) is planned for a future phase'",
    "build": "echo 'Mobile app (React Native) is planned for a future phase'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
