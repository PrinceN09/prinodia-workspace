{
  "name": "@govsphere/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "GovSphere Desktop Application — Tauri (planned)",
  "scripts": {
    "dev": "echo 'Desktop app (Tauri) is planned for a future phase'",
    "build": "echo 'Desktop app (Tauri) is planned for a future phase'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist target"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
