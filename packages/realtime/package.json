{
  "name": "@prinodia/realtime",
  "version": "1.2.0",
  "private": true,
  "description": "Prinodia Workspace real-time client SDK — WebSocket, presence, notifications, activity",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^20.14.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true }
  }
}
